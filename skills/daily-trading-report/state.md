# 当前状态

> 本文件是当前快照，goal 迭代时更新。每次 goal 完成后重置为最新状态。

## 当前快照（2026-06-21）

| 项目 | 状态 | 证据 |
|------|------|------|
| 主模板标题顺序 | 已验证 | `uv run python test_template_contract.py` 通过；生成文件标题抽取顺序符合 `template-contract.md` |
| 标题区日期格式 | 已验证 | 生成 HTML 标题区显示 `2026年6月17日（周三）`，输出文件名仍为 `reports/2026-06-17-daily.html` |
| `01 交易数据汇总` 分组 | 已验证 | `交易数据汇总`、`交易笔数`、`交易金额` 已位于同一个主 section；`02` 前主 section 计数为 1 |
| `01 交易数据汇总` 表格结构 | 已验证 | 合同测试覆盖 `metric-table`、`笔数占比`、`amount-table`、`金额占比`；表格样式压缩为 11px/5px |
| 旧主栏目打断 | 已验证 | `reports/2026-06-17-daily.html` 与 `scripts/report_template.html` 未再出现旧主栏目名称 |
| 生成命令 | 已验证（轻量） | `uv run python generate_report.py --date 20260617 --no-charts` 生成 `reports/2026-06-17-daily.html` |
| 缺失数据占位 | 已渲染 | O32/API 数据不可用时显示 `暂无成交`、`暂无货币市场数据`、`外部短评暂未获取`、`暂无相关数据` 等占位 |
| QT 短评缺失占位 | 已验证 | 资金面、现券、一级发行短评缺失时统一返回 `暂无有效消息` |
| 权益市场分析 | 已验证 | 默认显示 `外部短评暂未获取`；传入外部短评时渲染为 `analysis-box` 并标注来源 |
| 资金市场分析结构 | 已验证 | 无资金事件数据时仍稳定显示 `公开市场操作`、`债券发行与到期`、`资金面状况`，并使用 `暂无相关数据` 占位 |
| 趋势/区间预测准确率 | 已验证 | 没有历史预测数据时统一显示 `历史预测数据不足，暂无法计算准确率`，不再用当日价格替代准确率 |
| 风险提示 | 已验证 | 主模板渲染为红色编号列表，生成 HTML 中有 3 条 `risk-index`，规则引擎风险表仍不进入主模板流 |
| 一级市场分析 | 已验证 | QT 一级发行短评先渲染为 `analysis-box` 分析摘要，再展示代表性信息列表；发行结构分析保持 `暂无相关数据` |
| 窄幅长图视觉骨架 | 已验证（轻量截图） | 已改为 `max-width: 680px`、左侧时间线、白色内容块、蓝色标题标签、蓝色斜纹背景；Edge/Chrome headless 截图 `680x2400` 无明显顶部重叠 |

## 待完成

- [ ] `交易数据汇总`、`交易笔数`、`交易金额` 已完成主分组和基础表格拆分；后续需在真实 O32 数据可用时继续校验字段口径和图表位置。
- [ ] `趋势预测准确率`、`区间预测准确率` 缺少历史预测数据源，当前按合同降级占位。
- [ ] `资金市场分析` 已有稳定子结构和统一分析盒；真实公开市场操作字段仍需待资金事件 API 可用后校验。
- [ ] `权益市场分析` 已具备外部短评输入通道和失败降级；仍需由调用方在 AI 执行期提供真实 WebSearch 摘要。
- [ ] `一级市场分析` 已用 QT 一级发行短评形成分析摘要，发行结构分析仍无外部数据源，按合同占位。
- [ ] 视觉细节已具备左侧时间线、长图比例、红色风险提示、更密集表格和蓝色斜纹背景；已完成轻量截图检查，仍需最终完整长图验收。
