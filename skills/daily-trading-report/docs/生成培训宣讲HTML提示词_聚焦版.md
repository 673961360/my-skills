# 生成聚焦版交易部培训宣讲 HTML 的提示词

> 使用方式：将本提示词交给本地 Claude，并结合项目里的 API 接口文档、交易日报样例图、日报目标说明、Skills 文件结构继续润色。  
> 目标：生成一个适合基金公司交易部内部培训使用的单页宣讲 HTML。  
> 重要要求：不要做成“什么都讲到”的材料，要聚焦讲透三件事：  
> 1. AI 如何通过 API / CLI 取生产数据；  
> 2. AI 如何把结构化数据加工成指标和自然语言观点；  
> 3. 如何用交易日报样例沉淀成可复用 Skills 流程。

---

## 你要扮演的角色

你是一名资深金融科技产品经理、AI Agent 应用架构师、前端视觉设计师，熟悉基金公司交易业务、交易系统数据流、API 网关、CLI 工具化生产系统、结构化数据分析、日报/周报自动生成、Claude Code / Skills 工作流。

请生成一个完整的内部培训宣讲 HTML 页面，用于基金公司 IT 人员给交易部同事做约 30 分钟培训。

---

## 培训主题

**AI如何取数、用数，并沉淀成交易部开发提效流程**

副标题：

**以交易日报自动生成为例：从结构化数据到自然语言观点，再到可复用 Skills**

---

## 培训背景

我是基金公司 IT 人员，需要给交易部同事做一次约 30 分钟培训。

交易部同事未来不只是“问 AI 问题”，还需要理解如何借助 AI 进行开发提效，例如：

1. 用 AI 梳理一个业务场景需要哪些数据；
2. 用 AI 设计 API / CLI 调用流程；
3. 用 AI 生成数据加工脚本；
4. 用 AI 把结构化指标改写成交易观点；
5. 用 AI 沉淀 Skills，让日报、复盘、对手分析等高频工作可复用。

这次培训不要泛泛讲很多 AI 场景，而要讲透一条主线：

```text
自然语言需求
→ 任务拆解
→ API / CLI 取数
→ 数据加工
→ 指标分析
→ 观点生成
→ Skills 沉淀
→ 人工复核
```

---

## 不要展开的内容

请避免把页面写散。

不要重点讲：

1. 大模型原理；
2. 安全治理体系；
3. 权限、审计、脱敏的完整方案；
4. 所有交易系统介绍；
5. 很多泛泛的 AI 应用场景；
6. 过度炫酷但没有业务解释的视觉效果。

安全、权限、审计只在边界说明里一句话带过即可。

---

## 核心观点

页面要反复突出以下三句话：

```text
AI 提效的关键不是“提示词漂亮”，而是任务可拆、数据可取、口径可验收。
AI 的价值不是简单查数，而是把结构化数据加工成交易员能理解、能复核、能修改的业务观点。
一次性问答解决一次问题，可复用 Skills 才能持续提升效率。
```

---

## 页面风格要求

1. 金融科技风格，专业、清爽、克制。
2. 主色调：蓝色、白色、浅灰；可少量使用青色作为科技感点缀。
3. 页面不是传统 PPT，而是适合投屏讲解的长页面 HTML。
4. 内容必须有重点，最多分成 6 个大区块。
5. 每个区块都有一句“本节讲透什么”。
6. 大量使用流程图、卡片、三栏对比、代码块、表格。
7. 不要大段堆文字。
8. 适合飞书文档中预览，也适合本地浏览器打开宣讲。
9. 不依赖外部框架、外部 CDN、外部图片、外部字体。
10. 代码结构清晰，方便后续修改。

---

## 技术输出要求

请输出完整 HTML 单文件代码，包含：

- HTML
- CSS
- 必要的原生 JavaScript

要求：

1. 单文件可运行；
2. 不依赖外部资源；
3. 可以使用 CSS 绘制流程图、卡片、箭头、数据流背景；
4. 可以加入右侧目录导航；
5. 可以加入滚动进度条；
6. 可以加入讲师提示框；
7. 可以加入打印样式；
8. 页面中文显示良好；
9. 不要输出解释文字，只输出完整 HTML 代码。

---

# 页面结构要求

请严格按照以下结构生成，不要额外扩展太多模块。

---

## 1. 封面区：这次到底讲什么

标题：

```text
AI如何取数、用数，并沉淀成交易部开发提效流程
```

副标题：

```text
以交易日报自动生成为例：从结构化数据到自然语言观点，再到可复用 Skills
```

封面要突出一句话：

```text
不是让 AI 替代交易判断，而是让 AI 先把取数、计算、画图、写初稿这些重复工作做起来。
```

视觉建议：

- 使用科技感渐变背景；
- 展示核心流程：自然语言需求 → API取数 → 指标加工 → 观点生成 → Skills沉淀；
- 不要放太多文字。

---

## 2. 为什么要做：交易部真正的效率瓶颈

本节讲透什么：

```text
交易部不是缺数据，而是缺少把数据快速变成结论和材料的自动化流程。
```

展示传统工作链路：

```text
查多个系统 → 导出数据 → 拼 Excel → 算指标 → 画图 → 写日报/复盘 → 人工校验
```

展示 AI 辅助链路：

```text
AI 接收任务 → AI 调用 API / CLI → AI 加工指标 → AI 生成观点和页面 → 人工审核
```

强调：

```text
AI 开发提效不是“让 AI 随便聊”，而是把高频工作拆成可执行、可复用的流程。
```

视觉建议：

- 左右对比卡片；
- 左边叫“人工链路”，右边叫“AI辅助链路”；
- 用颜色突出减少的重复劳动。

---

## 3. 重点一：AI 如何取数

本节讲透什么：

```text
自然语言问题必须先变成可执行的数据任务，AI 才能稳定工作。
```

示例自然语言任务：

```text
帮我生成 2026 年 6 月 17 日交易日报。
```

AI 的拆解：

```text
1. 识别目标：生成交易日报
2. 判断模块：交易概览、指令成交、持仓变化、市场资讯、风险提示
3. 判断数据：指令、成交、持仓、行情、资讯、对手方、聊天消息
4. 调用工具：通过 API / CLI 查询生产系统
5. 返回结果：结构化 JSON / CSV / 表格
```

示例 CLI 代码块：

```bash
trade-cli orders --date 2026-06-17
trade-cli trades --date 2026-06-17
trade-cli positions --date 2026-06-17
trade-cli market --date 2026-06-17
trade-cli news --date 2026-06-17
trade-cli counterparties --date 2026-06-17
trade-cli chat-summary --date 2026-06-17
```

请重点展示“交易同事如何配合 AI 取数”。

三张卡片：

### 卡片1：说清问题边界

```text
不要只说：帮我分析一下交易情况。
更好的说法：帮我分析 2026-06-17 交易部日报，重点看指令执行、成交金额结构、未成交原因、持仓变化和需要关注的风险点。
```

### 卡片2：说清数据范围

```text
日期范围：当天 / 最近一周 / 最近一个月
业务范围：现券 / 回购 / 基金 / 一揽子
产品范围：全部产品 / 某产品 / 某产品组
输出范围：日报 / 复盘 / 异常提示 / 对手分析
```

### 卡片3：说清验收口径

```text
成交金额按什么单位展示？
成交率怎么算？
未成交是否包含撤单？
异常判断阈值是什么？
观点是日报风格还是复盘风格？
```

视觉建议：

- 中间放一个 AI Agent 节点；
- 左边是自然语言需求；
- 右边是 CLI 调用和返回数据；
- 下方放“交易同事需要补充的三类信息”。

---

## 4. 重点二：AI 如何把结构化数据生成观点

本节讲透什么：

```text
AI 不是把表格翻译成中文，而是要按“数据 → 指标 → 判断 → 观点”的链路工作。
```

请用三栏展示：

### 第一栏：原始数据

| 日期 | 业务类型 | 指令状态 | 成交金额 |
|---|---|---|---:|
| 6月17日 | 现券买入 | 已成交 | 5000万 |
| 6月17日 | 现券卖出 | 已成交 | 8000万 |
| 6月17日 | 回购 | 已成交 | 2亿 |
| 6月17日 | 现券买入 | 未成交 | 3000万 |

### 第二栏：加工指标

```text
总指令数：4笔
已成交：3笔
未成交：1笔
成交率：75%
总成交金额：3.3亿
回购成交金额占比：60.6%
现券成交金额占比：39.4%
未成交集中在现券买入方向
```

### 第三栏：自然语言观点

```text
今日交易执行整体较顺畅，4笔指令中3笔完成成交，成交率为75%。从金额结构看，回购业务贡献主要成交量，占比约六成，现券交易占比约四成。未成交指令集中在现券买入方向，后续可重点关注相关券种报价是否偏离目标收益率，或市场流动性是否不足。
```

然后重点讲三个观点模板，不要展开太多。

### 模板一：总量 + 结构 + 变化

```text
今日【指标】整体【上升/下降/平稳】，主要由【业务/产品/券种】贡献。
从结构看，【A类】占比最高，【B类】相对较低。
与【昨日/近5日均值/上周】相比，主要变化体现在【变化点】。
```

示例：

```text
今日成交金额整体较昨日上升，主要由回购业务放量贡献。从结构看，回购成交占比最高，现券成交相对平稳。与近5日均值相比，今日成交放量更多体现为资金类业务需求增加，而非现券换手显著提升。
```

### 模板二：异常 + 证据 + 可能原因

```text
今日【对象】出现【异常现象】。
从数据看，【证据1】、【证据2】。
可能原因包括【市场因素/报价因素/产品调仓/流动性因素】。
建议后续关注【需要跟踪的指标或事项】。
```

示例：

```text
今日部分现券买入指令未能成交。从数据看，未成交主要集中在买入方向，且目标收益率与市场成交收益率存在一定差距。可能原因包括报价条件较难达成、相关券种二级流动性不足，或市场收益率波动导致对手报价收敛。建议后续关注该类券种报价分布和估值偏离情况。
```

### 模板三：结果 + 归因 + 后续关注

```text
今日【结果】。
从归因看，主要受到【因素1】和【因素2】影响。
对组合或交易执行的影响是【影响】。
后续需要关注【指标/风险/机会】。
```

示例：

```text
今日产品持仓久期小幅下降。从归因看，主要受到长久期利率债减持和短端品种配置增加影响。对组合而言，久期下降有助于降低利率波动敏感度，但也可能压缩后续收益弹性。后续需要关注收益率曲线变化及长端资产再配置机会。
```

视觉建议：

- 先展示“表格 → 指标 → 观点”的三栏流程；
- 再展示三张观点模板卡片；
- 观点模板要突出，让交易同事能拿回去直接用。

---

## 5. 重点三：交易日报 Skills 如何沉淀

本节讲透什么：

```text
交易日报不是一次性让 AI 写一篇文章，而是把取数、指标、观点、复核规则沉淀成可重复执行的 Skill。
```

### 5.1 不要一上来复刻整张日报

强调：

```text
先稳定生成核心日报，再逐步补齐视觉和细节。
```

核心日报先拆五个模块：

```text
模块一：交易概览
模块二：指令与成交
模块三：持仓与产品变化
模块四：市场与资讯影响
模块五：风险提示与人工复核项
```

### 5.2 每个模块固定五个问题

```text
1. 这个模块需要哪些数据？
2. 数据从哪个接口 / CLI 取？
3. 需要计算哪些指标？
4. AI 应该生成什么观点？
5. 哪些结论必须人工复核？
```

给出示例：指令与成交模块

```text
数据：指令表、成交表
接口：orders、trades
指标：指令数、成交数、成交率、成交金额、未成交方向、业务类型占比
观点：今日执行是否顺畅，主要成交来自哪里，未成交集中在哪些方向
复核：成交率口径、撤单是否计入未成交、金额单位是否正确
```

### 5.3 trade-daily-report Skill 文件结构

展示如下结构：

```text
trade-daily-report/
├── README.md                 # Skill 说明：什么时候用，输入输出是什么
├── template.md               # 日报结构模板
├── data_requirements.md      # 每个模块需要的数据和字段
├── metrics_rules.md          # 指标计算口径
├── viewpoint_rules.md        # 观点生成规则和示例
├── tool_mapping.md           # 模块与 API / CLI 工具映射关系
├── review_checklist.md       # 人工复核清单
└── examples/
    ├── sample_input.json
    ├── sample_metrics.json
    └── sample_output.md
```

### 5.4 Skill 的一次执行流程

```text
Step 1：读取日报目标和日期
Step 2：根据模板判断需要哪些模块
Step 3：根据模块调用对应 CLI / API
Step 4：清洗字段并统一单位
Step 5：计算指标
Step 6：按观点模板生成文字
Step 7：输出日报初稿
Step 8：列出人工复核项
```

请突出：

```text
最关键的不是最后一步“生成文字”，而是：
取对数据、算对指标、按业务逻辑解释指标。
```

视觉建议：

- 用文件树展示 Skill 结构；
- 用横向流程图展示 Skill 执行过程；
- 用重点卡片突出“先核心后美化”。

---

## 6. 交易同事如何用 Claude 做开发提效

本节讲透什么：

```text
交易同事不一定亲自写代码，但要学会把业务目标、样例、数据口径、验收标准交给 AI，让 AI 协助开发。
```

展示四个实际用法，每个用一张卡片即可。

### 6.1 让 Claude 梳理接口调用流程

```text
输入：日报样例 + API 文档 + 目标输出
产出：每个模块需要调用哪些接口、字段和参数
```

### 6.2 让 Claude 写数据加工脚本

```text
输入：字段样例 + 指标口径
产出：Python / SQL / CLI 调用脚本，用于计算成交率、金额占比、变化等指标
```

### 6.3 让 Claude 沉淀 Skills 文档

```text
输入：人工日报样例 + 已验证口径
产出：template.md、metrics_rules.md、viewpoint_rules.md、review_checklist.md
```

### 6.4 让 Claude 生成日报页面

```text
输入：指标结果 + 日报模板 + 样式要求
产出：HTML / Markdown / 长图日报初稿
```

然后给一个“好请求”的示例：

```text
请基于交易日报样例，帮我设计 trade-daily-report Skill。
要求：
1. 先拆核心模块，不要追求一次性复刻完整长图；
2. 每个模块说明需要哪些 API 数据；
3. 每个指标说明计算口径；
4. 每段观点说明生成规则；
5. 最后输出人工复核清单。
```

视觉建议：

- 四张卡片；
- 最后放一个高亮的“好请求示例”代码块。

---

## 7. 边界和收尾

本节讲透什么：

```text
AI 负责初稿和流程沉淀，人负责判断和最终确认。
```

展示三栏：

```text
AI：取数、加工、生成初稿、沉淀流程
系统：提供数据、接口、权限、口径、审计
人：判断业务含义、修正观点、确认发布
```

结尾金句：

```text
一次性问答解决一次问题；
可复用 Skills 才能持续提升效率。
```

最后再强调：

```text
日报只是起点。
同样的方法可以扩展到交易复盘、持仓解释、对手分析、资讯联动。
但今天最重要的是先把“取数 → 指标 → 观点 → Skill”这条链路跑通。
```

---

## 版式与交互建议

请在 HTML 中实现：

1. 顶部滚动进度条；
2. 右侧固定目录导航；
3. 每个大章节有编号和“本节讲透什么”提示；
4. 关键流程使用横向箭头图；
5. “表格 → 指标 → 观点”使用三栏卡片；
6. “观点模板”使用三张重点卡片；
7. “Skill 文件结构”使用代码块或文件树卡片；
8. “讲师提示”使用浅色提示框；
9. 结尾金句使用大号字体；
10. 打印样式适合导出 PDF。

---

## 最终输出要求

请直接输出完整 HTML 代码。

不要解释设计思路。  
不要输出额外说明。  
不要使用外部资源。  
不要引用外部图片。  
不要使用外部 CDN。  
不要使用 Markdown 包裹代码块。  
